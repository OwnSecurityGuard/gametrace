# http-decoder 本地运行环境变量模板（复制为 .env 后按本机改值；.env 不入库）。
# ⚠️ Go 程序不会自动读 .env，仓库里也没有 dotenv 加载器：必须先 source，
#    或把变量直接前置在启动命令里。只在 shell 里 `set X=Y`（Git Bash）不等于
#    设置环境变量，插件仍会回落到默认 :9091。

# 平台 registry 的【对外可达】地址。裸机自启 pipeline 默认 :9091；
# docker-compose 部署下容器内 9091 映射到宿主 19091，本机插件用 127.0.0.1:19091。
# 以 MCP get_registry_addr / get_plugin_env 返回值为准。
GT_REGISTRY_ADDR=127.0.0.1:19091

# 隧道标记：平台唯一运行模式，由运行方注入，插件代码不读取不分支。
GT_TUNNEL=1

# 注册鉴权 Bearer token：宿主配置了 GT_AUTH_TOKENS 时【必填】，缺失会报
# rpc error: code = PermissionDenied desc = invalid or missing auth token。
# 值取自仓库根 .env 的 GT_AUTH_TOKENS（格式 owner=gt_tok_xxx[:admin]，逗号分隔）；
# 冒号后的 :admin 是角色后缀，不属于 token。这里填占位值，别把真实 token 入库。
GT_AUTH_TOKEN=gt_tok_change_me

# 启动示例（Git Bash / bash）：
#   set -a; . ./.env; set +a; ./http-decoder.exe
#   # 或单次前置：GT_REGISTRY_ADDR=127.0.0.1:19091 GT_TUNNEL=1 GT_AUTH_TOKEN=<tok> go run .
# 启动示例（PowerShell）：
#   $env:GT_REGISTRY_ADDR="127.0.0.1:19091"; $env:GT_TUNNEL="1"; $env:GT_AUTH_TOKEN="<tok>"; .\http-decoder.exe
# 启动示例（cmd.exe，set 在这里才有效，但不同命令行不共享）：
#   set GT_REGISTRY_ADDR=127.0.0.1:19091 && set GT_TUNNEL=1 && set GT_AUTH_TOKEN=<tok> && http-decoder.exe
