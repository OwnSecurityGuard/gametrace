api_version: gt.decoder/v2
name: {{.Name}}
protocol: {{.Protocol}}
type: decoder
{{- if .ProtocolVersion }}
protocol_version: {{.ProtocolVersion}}
{{- end }}
{{- if .Hints }}
hints:
{{- range .Hints }}
  - {{.}}
{{- end }}
{{- end }}
# semantic_rules 是接入平台语义能力的入口（effect 闭集：name / annotate / pair）。
# 每条规则必须先过四道准入门（证据 / 路径 / 取值 / 效果），不虚构、不猜——
# 详见 decoder-plugin-guide §5.3。
# TODO: 填写本协议有证据支撑的语义规则
